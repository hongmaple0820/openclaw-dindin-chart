# OpenClaw 钉钉聊天集成

让多个 AI 机器人（基于 OpenClaw）在钉钉群中与人类实时聊天、互相对话。

## 📦 项目组成

```
openclaw-dindin-chart/
├── chat-hub/          # 消息中转服务（核心）
├── clawdbot-dingtalk/ # 钉钉插件（可选）
└── README.md
```

## ✨ 功能特性

- 🚀 **实时触发** - 收到消息立即触发 OpenClaw，无需等待心跳
- 🤖 **多机器人** - 支持小琳、小猪等多个 AI 同时在线
- 💬 **双向通信** - 接收钉钉消息 + 发送回复到钉钉
- 📡 **Redis 消息总线** - 多机器人共享消息，跨机器通信
- 🔒 **配置隔离** - 每个机器人独立配置，互不干扰

## 🏗️ 架构

```
┌──────────────┐     Webhook      ┌──────────────┐
│   钉钉群      │ ◄──────────────► │   chat-hub   │
└──────────────┘                  └──────┬───────┘
                                         │
                                         ▼ Redis Pub/Sub
                                  ┌──────────────┐
                                  │    Redis     │
                                  └──────┬───────┘
                                         │
                    ┌────────────────────┼────────────────────┐
                    ▼                    ▼                    ▼
             ┌──────────────┐    ┌──────────────┐    ┌──────────────┐
             │ chat-hub     │    │ chat-hub     │    │    ...       │
             │ (小琳)       │    │ (小猪)       │    │              │
             └──────┬───────┘    └──────┬───────┘    └──────────────┘
                    ▼                    ▼
             ┌──────────────┐    ┌──────────────┐
             │  OpenClaw    │    │  OpenClaw    │
             │  (小琳)      │    │  (小猪)      │
             └──────────────┘    └──────────────┘
```

## 🚀 快速开始

详细说明请查看 [chat-hub/README.md](./chat-hub/README.md)

### 1. 克隆项目

```bash
cd ~/.openclaw
git clone https://gitee.com/hongmaple/openclaw-dindin-chart.git
cd openclaw-dindin-chart/chat-hub
npm install
```

### 2. 创建本地配置

```bash
cat > config/local.json << 'EOF'
{
  "bot": {
    "name": "你的机器人名",
    "local": true
  },
  "dingtalk": {
    "webhookBase": "https://oapi.dingtalk.com/robot/send?access_token=你的token",
    "secret": "你的密钥"
  }
}
EOF
```

### 3. 启动服务

```bash
npm start
```

### 4. 配置钉钉 Outgoing

设置 Outgoing 地址: `http://你的服务器IP:3000/webhook/dingtalk`

## 📚 文档

- [chat-hub 详细文档](./chat-hub/README.md) - 完整的配置和使用说明
- [钉钉机器人文档](https://open.dingtalk.com/document/orgapp/custom-robot-access) - 官方文档

## ⚠️ 多机器人配置

每个机器人需要：
1. 自己的钉钉 Webhook 机器人（不同的 token 和 secret）
2. 自己的 `config/local.json` 配置
3. 运行自己的 chat-hub 实例

配置文件 `local.json` 在 `.gitignore` 中，不会被 `git pull` 覆盖。

## 🤝 机器人列表

| 机器人 | 标识 | 运行环境 |
|--------|------|----------|
| 小琳 | maple-bot | WSL2 |
| 小猪 | lin-bot | Ubuntu 虚拟机 |

## 📄 许可证

MIT

---

*基于 OpenClaw + Redis + 钉钉 Outgoing 构建*
