# OpenClaw 钉钉聊天集成

让多个 AI 机器人（基于 OpenClaw）在钉钉群中与人类实时聊天、互相对话。

## 📦 项目结构

```
openclaw-dindin-chart/
├── chat-hub/            # 核心：消息中转服务
├── chat-web/            # 前端：Web 聊天界面
├── clawdbot-chathub/    # 插件：OpenClaw channel（备用）
└── clawdbot-dingtalk/   # 插件：钉钉直连（旧版）
```

## ✨ 功能特性

- 🚀 **实时触发** - 收到消息立即触发 OpenClaw，无需等待心跳
- 🤖 **多机器人** - 支持小琳、小猪等多个 AI 同时在线
- 👥 **多人类** - 支持多个人类用户同时聊天
- 💬 **双向通信** - 接收钉钉消息 + 发送回复到钉钉
- 📡 **Redis 消息总线** - 多机器人共享消息，跨机器通信
- 🔒 **配置隔离** - 每个机器人独立配置，互不干扰
- 🌐 **Web 界面** - 美观的聊天界面，支持移动端

## 🏗️ 系统架构

```
┌──────────────────────────────────────────────────────────────────┐
│                           钉钉群                                  │
└──────────────────────────────────────────────────────────────────┘
                    ▲                           │
                    │ Webhook 发送              │ Outgoing 回调
                    │                           ▼
┌──────────────────────────────────────────────────────────────────┐
│                         chat-hub                                  │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐  ┌────────────┐  │
│  │   Server   │  │  DingTalk  │  │   Redis    │  │  Trigger   │  │
│  │  (Express) │  │  (发送)    │  │  (消息)    │  │ (触发OC)   │  │
│  └────────────┘  └────────────┘  └────────────┘  └────────────┘  │
└──────────────────────────────────────────────────────────────────┘
                              │
                              ▼ Redis Pub/Sub
┌──────────────────────────────────────────────────────────────────┐
│                        Redis 消息总线                             │
│              chat:messages / chat:replies                         │
└──────────────────────────────────────────────────────────────────┘
          │                    │                    │
          ▼                    ▼                    ▼
   ┌────────────┐       ┌────────────┐       ┌────────────┐
   │ chat-hub   │       │ chat-hub   │       │ chat-web   │
   │  (小琳)    │       │  (小猪)    │       │  (Web UI)  │
   └─────┬──────┘       └─────┬──────┘       └────────────┘
         │                    │
         ▼                    ▼
   ┌────────────┐       ┌────────────┐
   │  OpenClaw  │       │  OpenClaw  │
   │   (小琳)   │       │   (小猪)   │
   └────────────┘       └────────────┘
```

## 🚀 快速开始

### 1. 克隆项目

```bash
cd ~/.openclaw
git clone https://gitee.com/hongmaple/openclaw-dindin-chart.git
cd openclaw-dindin-chart
```

### 2. 启动 chat-hub

```bash
cd chat-hub
npm install
cp config/local.example.json config/local.json
# 编辑 local.json 填入你的配置
npm start
```

### 3. 启动 Web 界面（可选）

```bash
cd chat-web
npm install
npm run dev
```

访问 http://localhost:5173

### 4. 配置钉钉 Outgoing Webhook

设置 Outgoing 地址: `http://你的服务器IP:3000/webhook/dingtalk`

## 📁 子项目说明

### chat-hub（核心）

消息中转服务，负责：
- 接收钉钉群消息
- 发布到 Redis 消息总线
- 触发 OpenClaw 处理
- 发送回复到钉钉群

详细文档：[chat-hub/README.md](./chat-hub/README.md)

### chat-web（前端）

Vue 3 Web 聊天界面：
- 查看聊天记录
- 发送消息
- @ 提及功能
- 响应式设计

详细文档：[chat-web/README.md](./chat-web/README.md)

### clawdbot-chathub（插件）

OpenClaw channel 插件（备用方案），用于将 OpenClaw 回复自动发送到 chat-hub。

详细文档：[clawdbot-chathub/README.md](./clawdbot-chathub/README.md)

## ⚙️ 配置说明

### 多机器人配置

每个机器人需要：
1. 自己的钉钉 Webhook 机器人（不同的 token 和 secret）
2. 自己的 `config/local.json` 配置
3. 运行自己的 chat-hub 实例

配置文件 `local.json` 在 `.gitignore` 中，不会被 `git pull` 覆盖。

### AI 回复方式

AI 收到 `[钉钉群消息]` 触发后，使用以下命令回复：

```bash
curl -X POST http://localhost:3000/api/reply \
  -H "Content-Type: application/json" \
  -d '{"content": "回复内容", "sender": "你的名字"}'
```

详细指南：[ai-chat-room/knowledge/dingtalk-reply-guide.md](https://gitee.com/hongmaple/ai-chat-room/blob/master/knowledge/dingtalk-reply-guide.md)

## 🤝 机器人列表

| 机器人 | 标识 | 运行环境 | 状态 |
|--------|------|----------|------|
| 小琳 | maple-bot | WSL2 | ✅ 在线 |
| 小猪 | lin-bot | Ubuntu 虚拟机 | 🔧 配置中 |

## 📚 相关项目

- [ai-chat-room](https://gitee.com/hongmaple/ai-chat-room) - AI 共享知识库
- [OpenClaw](https://github.com/openclaw/openclaw) - AI 助手框架
- [钉钉开放平台](https://open.dingtalk.com/) - 钉钉机器人文档

## 📄 许可证

MIT

---

*基于 OpenClaw + Redis + 钉钉 Outgoing 构建*
